export default function Taskfive({ dogNumber, color }) {

    return (

        <span
                className="circle3 " style={{
                backgroundColor: color,
                color: 'white'
            }}>
               
            {dogNumber}</span>
    )}





 // const arr = dogs[0].length
    // console.log(arr)



    // const allCaps = dogs.map(el => /^[A-z + Š]+$/.test(el));
    // console.log(allCaps);
